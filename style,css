/* Corpo do site */
body {
    background-color: #121212; /* Fundo escuro */
    color: #f0f0f0; /* Texto claro */
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
}

/* Cabeçalho */
header {
    background-color: #1f1f1f; /* Fundo do cabeçalho */
    padding: 20px;
    text-align: center;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra */
}

header h1 {
    color: #4CAF50; /* Verde vibrante */
    font-size: 2.5rem;
}

/* Seções principais */
main {
    max-width: 900px;
    margin: 20px auto;
    padding: 20px;
}

/* Cada filme */
.movie {
    background-color: #1e1e1e; /* Fundo de cada seção */
    border-radius: 10px;
    padding: 15px;
    margin-bottom: 20px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2); /* Sombra suave */
}

/* Título dos filmes */
.movie h2 {
    color: #4CAF50; /* Verde */
    font-size: 1.8rem;
    margin-bottom: 10px;
}

/* Conteúdo dos filmes */
.movie-content {
    display: flex;
    align-items: center;
    gap: 20px;
}

/* Imagens */
.movie-content img {
    width: 150px;
    height: auto;
    border-radius: 8px; /* Bordas arredondadas */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Sombra suave */
}

/* Texto */
.movie-content p {
    font-size: 1rem;
    line-height: 1.6;
}

/* Rodapé */
footer {
    text-align: center;
    padding: 15px;
    background-color: #1f1f1f;
    color: #f0f0f0;
    font-size: 0.9rem;
    border-top: 2px solid #4CAF50;
}

